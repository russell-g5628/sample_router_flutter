import 'package:flutter/material.dart';

class BindingWithRoutePage extends StatelessWidget {
  const BindingWithRoutePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Binding With Route')),
      body: Container(),
    );
  }
}
